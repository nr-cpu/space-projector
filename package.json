{
  "name": "skylight",
  "version": "0.1.0",
  "private": true,
  "description": "space.projector — fork of cpaczek/skylight, projecting overhead aircraft and a full-sky satellite view onto your ceiling in real time.",
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev": "concurrently -n server,web,tracker -c green,cyan,magenta \"pnpm dev:server\" \"pnpm dev:web\" \"pnpm dev:tracker\"",
    "dev:server": "pnpm -F server dev",
    "dev:web": "pnpm -F web dev",
    "dev:tracker": "pnpm -F tracker dev",
    "build": "pnpm -F web build",
    "start": "pnpm -F server start",
    "test": "vitest run",
    "typecheck": "tsc -p shared/tsconfig.json && pnpm -F server typecheck && pnpm -F web typecheck && pnpm -F tracker typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "playwright": "^1.60.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
